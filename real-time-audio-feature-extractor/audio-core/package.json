{
  "name": "audio-core",
  "version": "0.1.0",
  "description": "Rust/WASM audio feature extraction core",
  "main": "audio_core.js",
  "types": "audio_core.d.ts",
  "scripts": {
    "build": "wasm-pack build --target web --out-dir pkg",
    "test": "cargo test"
  },
  "devDependencies": {
    "wasm-pack": "^0.12.1"
  }
}